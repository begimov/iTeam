import { mapActions, mapGetters } from 'vuex'

export default {
    props: {
        test: {
            type: Object,
            required: true
        }
    },
    methods: {
        multipleСhoice(question) {
            return true
        }
    }
};